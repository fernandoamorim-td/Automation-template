@Library("talkdesk-default-pipeline") _
scriptedPipeline {
    name      = 'template-smoketests'
    scmUrl    = 'template-github-project-link'
    deploy           = [
        [name: 'template', namespace: 'template']
    ]
}